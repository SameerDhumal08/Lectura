# Lectura — Full Stack

- `frontend/` — React (Vite) UI with mock data. Run: `cd frontend && npm install && npm run dev`
- `backend/` — Spring Boot REST API (JWT auth, JPA). Run: `cd backend && ./mvnw spring-boot:run`

See each folder's README for details.
